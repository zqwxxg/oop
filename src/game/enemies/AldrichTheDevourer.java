package game.enemies;

public class AldrichTheDevourer extends LordOfCinder{
    /**
     * Constructor.
     *
     * @param name
     * @param displayChar
     * @param hitPoints
     * @param soulCount
     */
    public AldrichTheDevourer(String name, char displayChar, int hitPoints, int soulCount) {
        super(name, displayChar, hitPoints, soulCount);
    }
}
